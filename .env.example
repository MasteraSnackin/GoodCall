# Optional server-only configuration. Never use a VITE_ prefix for credentials.
# Alternatively use Set up AI in the app for an in-memory server session.
AI_PROVIDER=anthropic
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-haiku-4-5-20251001
# Optional alternative: select AI_PROVIDER=openai and set the matching key.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
